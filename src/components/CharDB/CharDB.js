import React from 'react';

const charDB = (props) =>{

const body =[];


console.log(body);
console.log(props.id);
  return (
    <div>
    <p>{props.id} : {props.class}</p>

    </div>
  );
}

export default charDB;
